import React, { memo } from "react";
import { StyleSheet, Text, View } from "react-native";

interface Props {
  itemList: [];
}

const FrontPageList = memo((props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
});
export default FrontPageList;

const styles = StyleSheet.create({});
